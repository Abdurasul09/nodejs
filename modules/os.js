const os = require('os');

// platform
console.log(os.platform());

// ifnormation
console.log(os.cpus());

// architector
console.log(os.arch());

// free memory
console.log(os.freemem());

// total memory
console.log(os.totalmem());

// entry point
console.log(os.homedir());
